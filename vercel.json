{
    "version": 2,
    "builds": [
        {
            "src": "backend/comparisons/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/backend/comparisons/(.*)",
            "dest": "/backend/comparisons/main.py"
        }
    ]
}